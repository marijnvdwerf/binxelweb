# Binxelview Web UI Layout

## Overall Layout

```
┌─────────────────────────────────────────────────────────────────────┐
│                              App                                    │
├──────────────────────┬──────────────────────────────────────────────┤
│      Sidebar         │              Viewer Container                │
│    (240px fixed)     │              (flex: 1)                       │
│                      │                                              │
│  ┌────────────────┐  │  ┌────────────────────────────────────────┐  │
│  │ Position       │  │  │                                        │  │
│  ├────────────────┤  │  │                                        │  │
│  │ Format         │  │  │                                        │  │
│  ├────────────────┤  │  │         Pixel Viewer Canvas            │  │
│  │ Bit Planes     │  │  │         (virtual scrolling)            │  │
│  ├────────────────┤  │  │                                        │  │
│  │ ▸ Tiling       │  │  │                                        │  │
│  ├────────────────┤  │  │                                        │  │
│  │ Palette        │  │  │                                        │  │
│  ├────────────────┤  │  └────────────────────────────────────────┘  │
│  │ Pixel Info     │  │                                              │
│  └────────────────┘  │                                              │
│                      │                                              │
└──────────────────────┴──────────────────────────────────────────────┘
```

---

## Panel Details

### Position Panel

```
┌─ Position ─────────────────────── ↓ ⊞ 🔗 ─┐
│                                            │
│  Offset              Zoom                  │
│  ┌──────────┬───┐    ┌───┬─────┬───┐      │
│  │ 00000000 │ +0│    │ - │  2  │ + │      │
│  └──────────┴───┘    └───┴─────┴───┘      │
│                                            │
└────────────────────────────────────────────┘
```

**Header actions (right side):**
| Icon | State | Description |
|------|-------|-------------|
| `↓` / `→` | Shows current | Layout direction (vertical/horizontal) |
| `⊞` | Active when ON | Grid padding visibility |
| `🔗` | Active when ON | Snap scroll to stride |

**Controls:**
- **Offset**: Byte position (hex) + bit offset (0-7)
- **Zoom**: 1-32x with +/- buttons

---

### Format Panel (PackingPanel)

```
┌─ Format ───────────────────────────────────┐
│                                            │
│  BPP                                       │
│  ┌────┐  🔗  ┌──────────┬───┐             │
│  │  4 │      │        0 │ +4│  ← pixel    │
│  └────┘      └──────────┴───┘    stride   │
│                                            │
│  Width                                     │
│  ┌────┐  🔗  ┌──────────┬───┐             │
│  │  8 │      │        4 │ +0│  ← row      │
│  └────┘      └──────────┴───┘    stride   │
│                                            │
│  Height                                    │
│  ┌────┐  🔗  ┌──────────┬───┐             │
│  │  8 │      │       32 │ +0│  ← next     │
│  └────┘      └──────────┴───┘    stride   │
│                                            │
└────────────────────────────────────────────┘
```

**Each row:**
- Left: Value input (BPP, Width, Height)
- Middle: Link icon 🔗 (toggles auto-calculation)
  - Linked (active): stride auto-calculated
  - Unlinked: manual stride entry
- Right: Stride as byte + bit offset

---

### Bit Planes Panel

```
┌─ Bit Planes ─────────────────── ⇄ ☰ ─┐
│                                       │
│    7   6   5   4   3   2   1   0     │  ← bit index
│  ┌───┬───┬───┬───┬───┬───┬───┬───┐   │
│  │ 7 │ 6 │ 5 │ 4 │ 3 │ 2 │ 1 │ 0 │   │  ← bit offset
│  └───┴───┴───┴───┴───┴───┴───┴───┘   │
│                                       │
│  (rows continue for bpp > 8)          │
│   15  14  13  12  11  10   9   8     │
│  ┌───┬───┬───┬───┬───┬───┬───┬───┐   │
│  │...│...│...│...│...│...│...│...│   │
│  └───┴───┴───┴───┴───┴───┴───┴───┘   │
│                                       │
└───────────────────────────────────────┘
```

**Header actions:**
| Icon | Active When | Description |
|------|-------------|-------------|
| `⇄` | `littleEndian=false` | Reverse bit order within bytes |
| `☰` | `chunky=true` | Chunky mode (contiguous bits) |

**Table:**
- Header row: bit index (descending: 7, 6, 5... 0)
- Value row: bit offset from pixel start
- Disabled (grey) when chunky mode is ON
- Right-aligned, empty slots on left for partial rows

---

### Tiling Panel (Collapsed by Default)

```
┌─ ▸ Tiling ─────────────────────────────────┐
└────────────────────────────────────────────┘

┌─ ▾ Tiling ─────────────────────────────────┐
│                                            │
│  Size                                      │
│  ┌─────────────┐  ┌─────────────┐          │
│  │ X │   0     │  │ Y │   0     │          │
│  └─────────────┘  └─────────────┘          │
│                                            │
│  Stride                                    │
│  ┌──────────┬───┐  ┌──────────┬───┐       │
│  │ X │   0  │+0 │  │ Y │   0  │+0 │       │
│  └──────────┴───┘  └──────────┴───┘       │
│                                            │
│  Pattern                                   │
│  ┌────────┬─────────┬─────────┐           │
│  │ Linear │ Z-Order │ N-Order │           │
│  └────────┴─────────┴─────────┘           │
│                                            │
└────────────────────────────────────────────┘
```

**Controls:**
- **Size X/Y**: Tile dimensions (0 = disabled)
- **Stride X/Y**: Byte + bit offset between tiles
- **Pattern**: Pixel ordering within tiles
  - Linear (default): Normal sequential
  - Z-Order: Morton curve (GPU textures)
  - N-Order: Rotated Morton curve

---

### Palette Panel

```
┌─ Palette ───────────────────────── ↻ ─┐
│                                        │
│  ┌──────────────────────────────────┐ │
│  │ RGB                            ▾ │ │
│  └──────────────────────────────────┘ │
│                                        │
│  ┌────────────────┐  ┌──────────────┐ │
│  │                │  │ [▓] Backgrnd │ │
│  │   Palette      │  │              │ │
│  │   Preview      │  │  [↑] [↓]    │ │
│  │   (clickable)  │  │  Load  Save  │ │
│  │                │  │              │ │
│  └────────────────┘  └──────────────┘ │
│                                        │
└────────────────────────────────────────┘
```

**Header action:**
- `↻` Refresh: Regenerate random palette (only when Random mode)

**Controls:**
- **Mode dropdown**: RGB, Random, Greyscale, Cubehelix, Custom
- **Palette preview**: 100x100 canvas, click to edit colors
- **Background swatch**: Click to change grid background color
- **Load/Save**: Import/export .pal files (RGB24 format)

---

### Pixel Info Panel

```
┌────────────────────────────────────────────┐
│  1234+3 = 15                               │
│  000004D2+3 = F                            │
└────────────────────────────────────────────┘
```

Appears at bottom of sidebar when hovering over pixels.
Shows:
- Line 1: Decimal byte+bit = decimal value
- Line 2: Hex byte+bit = hex value

---

## Viewer Area

```
┌────────────────────────────────────────────┐
│                                            │
│  ┌──┬──┬──┬──┐ ┌──┬──┬──┬──┐              │
│  │  │  │  │  │ │  │  │  │  │              │
│  ├──┼──┼──┼──┤ ├──┼──┼──┼──┤   Tile grid  │
│  │  │  │  │  │ │  │  │  │  │   with       │
│  ├──┼──┼──┼──┤ ├──┼──┼──┼──┤   1px gaps   │
│  │  │  │  │  │ │  │  │  │  │              │
│  └──┴──┴──┴──┘ └──┴──┴──┴──┘              │
│                                            │
│  ┌──┬──┬──┬──┐ ┌──┬──┬──┬──┐              │
│  │  │  │  │  │ │  │  │  │  │              │
│  ...                                       │
│                                            │
│                    (scrollable)            │
└────────────────────────────────────────────┘
```

**Features:**
- Virtual scrolling for large files
- Grid padding between tiles (toggleable)
- Zoom applies to entire canvas
- Hover to see pixel info in sidebar

**No file loaded:**
```
┌────────────────────────────────────────────┐
│                                            │
│        Drop a file here or press Ctrl+O   │
│                                            │
└────────────────────────────────────────────┘
```

---

## Icon Button Styles

### Toggle buttons (icon-btn toggle)
```
┌───┐     ┌───┐
│ ☐ │     │ ☑ │
└───┘     └───┘
 off      active (highlighted background)
```

### Auto-toggle buttons (auto-toggle-btn)
```
  ○         ●
 dim     darker on hover
 (no background, just icon color change)
```

---

## Keyboard Shortcuts

| Key | Action |
|-----|--------|
| `Ctrl+O` | Open file |
| `Ctrl+R` | Reload file |
| `←` `→` | Advance pixel |
| `↑` `↓` | Advance row |
| `PgUp` `PgDn` | Advance next (tile) |
| `+` `-` | Zoom in/out |
| `G` | Toggle grid |
